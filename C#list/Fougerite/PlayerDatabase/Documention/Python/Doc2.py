PlayerDatabase.GetName(Player) # returns Playername.

PlayerDatabase.GetNameFromID(id) # returns player name that matches by id

PlayerDatabase.IsAdmin(Player) #Returns true if player is set as admin in the database.

PlayerDatabase.SetData(Player, selection, data) # Edits a data valu